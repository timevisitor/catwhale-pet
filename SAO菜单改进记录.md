# SAO 菜单改进记录 · 2026-09-19

这次在现有项目上修复菜单并完善样式，沿用 Electron、角色素材、聊天、文件搜索和系统信息采集。

## 使用

双击 `dist/桌宠/桌宠.exe`，右键角色打开菜单。若旧进程还在运行，先从托盘退出旧版本再启动。

- 圆形图标或分类行均可切换分类；功能列表直接点击执行。
- 外观 → 显示大小进入子菜单，当前大小带勾选；调节后保持菜单展开。
- 子菜单上方的返回按钮或 Esc 返回上一级；再按 Esc 关闭。
- 上下方向键选择；Enter 执行；左右方向键按菜单实际展开方向进出。
- 菜单在屏幕边缘会翻转，在窄屏折叠角色卡；极窄时顶部“切换分类”逐项切换分类。
- “陪它忙起来”是 10 秒演示动作，会自动结束，不是提交真实 AI 任务。

## 修复

1. 点击 SAO 菜单和设置输入框被空白点击监听器提前关闭。
2. 重复的函数定义覆盖了菜单执行逻辑，导致功能点击失效。
3. 分类行与功能行的索引混用，使悬停触发错误分支。改为明确点击展开。
4. Esc 被多个监听器处理，一次按键连关多层。现在使用真实焦点并逐级返回。
5. 异步读取工作区后重新显示已经关闭的菜单；快速开关现在有请求序号保护。
6. 子菜单靠近底部时溢出屏幕；按实际子菜单尺寸限制位置，长列表可滚动。
7. 桌面宿主把显示器全局坐标直接交给页面；现在转换成窗口内容区坐标。
8. “忙起来”演示只增加任务计数，不结束；现在 10 秒自动结束且不能重复叠加。
9. 菜单修改大小后控制面板数值不同步；现在大小滑杆和标签同步。
10. 构建清单遗漏 harness-client、sysinfo、tools 和 harness-sdk；补齐既有运行依赖。

## 视觉

- 保留白灰半透明面板、金色当前分类、竖排圆形分类轴及横向分级菜单。
- 去除分类轴背后的整块白色底板，恢复独立浮动圆钮。
- 修正图标名称，外观、动作、工具和设置不再都显示同一个默认图标。
- 功能列表改为紧凑行，分类行与圆钮中心对齐。
- 角色预览固定在卡片内部，状态文字不再溢出；硬件数据有长文本限制和悬停完整提示。
- 样式独立为 `web/sao.css`；支持系统“减少动态效果”。

## 验证与边界

- 自动化浏览器检查：真实鼠标点击、返回层级、功能入口、异步关闭、六种窗口尺寸的四角布局、125%/150%/200% 设备缩放、命中检测与演示任务结束。
- 原有 `_interaction_test.html` 状态机、拖动、聊天、搜索及穿透回归套件通过。
- `node tools/check-channels.js` 通过，鼠标穿透 IPC 通道与原有两个切换位置均保留。
- 隔离 Electron 宿主测试通过：初始化、真实系统信息、大小分支、窗口工作区、关闭状态与空白命中检测。该测试使用软件渲染，以避开测试环境的 GPU 启动故障。
- 没有发送真实 DeepSeek 请求、修改开机自启或写入个人设置；聊天搜索测试覆盖菜单入口和现有回归逻辑，不代表外部服务的联网可用性。
- 没有通过真实鼠标跨应用点击来验证 Windows 穿透；已验证命中检测与原有 IPC 链路。
- 现有主窗口仍覆盖主显示器；工作区坐标修正不等于新增跨显示器拖动能力。

## 给 Hermes 的维护入口

- `web/index.html`：菜单状态、数据、真实功能、定位、鼠标与键盘交互。
- `web/sao.css`：独立主题与响应式布局。
- `app/main.js`：`pet:workarea` 返回窗口内工作区坐标。
- `app/package.json`：补齐打包依赖。
- `tools/check-sao.cjs`：菜单回归测试，需要 Node 和 Playwright。默认使用 Windows Edge；可通过 `PET_BROWSER` 指定浏览器，`PLAYWRIGHT_MODULE` 指定 Playwright 安装位置。
- `app/renderer/` 与 `dist` 内的页面是同步副本，后续先改 `web/`，再运行 `node app/tools/sync-renderer.js`。

本轮原文件备份在 `work/codex-sao-backup-20260919/`；同步发布时还会按原相对路径备份覆盖的文件和压缩包。不要删除备份，直到确认新版本可用。
